"""
⚠️  Ce fichier est obsolète — il a été déplacé vers src/donor_selection.py

Pour lancer uniquement le stage 2 (sélection du donor pool) :
    python run_pipeline.py --only stage2

Ou directement :
    python src/donor_selection.py
"""
raise ImportError(
    "donor_selection.py est obsolète. Utilise 'python run_pipeline.py' ou 'python src/donor_selection.py'."
)
